[package]
name = "multilingual-agent"
version = "0.1.0"
edition = "2024"

[dependencies]
adk-rust = "0.1.1"
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
dotenv = "0.15"
adk-session = "0.1.1"
